use a command to backup the root directory once per 3 nights
use a command to backup the home folder every night
tar and gzip the resulting stuff in the that should be
outputted to the temp folder in the Backup folder
make a structure where it will eventually hold a backup
of every month.
figure out how to connect with ssh to the pi
root pi command:
sudo rsync -aAXv -e ssh --exclude={"/dev/*","/proc/*","/sys/*","/tmp/*","/run/*","/mnt/*","/media/*","/lost+found","/home/*"} root@taaart:/ /mnt/data/Backup/temp???
home pi command:
sudo rsync -aAXv -e ssh --exclude={"/home/.cache/*???"} root@taaart:/home/alex /mnt/data/Backup/temp???
still need to figure out where exactly to store it
commands for backing up the pc
root pc command:
sudo rsync -aAXv --exclude={"/dev/*","/proc/*","/sys/*","/tmp/*","/run/*","/mnt/*","/media/*","/lost+found","/home/*"} / /mnt/data/Backup/temp???
home pc command:
sudo rsync -aAXv --exclude={"/home/.cache/*???"} /home/alex /mnt/data/Backup/temp???
